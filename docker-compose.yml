version: '3.8'

services:
  palabras-app:
    build: .
    container_name: palabras-olivia
    ports:
      - "3011:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
